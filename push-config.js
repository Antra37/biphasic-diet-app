// URL of the Cloudflare Worker that sends supplement reminders when the app is closed
// (see worker/README.md). Leave empty to only remind while the app is open.
window.SIBO_PUSH_API = "https://sibo-supplement-push.antra37.workers.dev";
